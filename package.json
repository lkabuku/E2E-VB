{
  "name": "e2e-vb",
  "version": "1.0.0",
  "description": "Tests de non-régression automatisés",
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright test --reporter=html",
    "test:ci": "playwright test --reporter=github"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
